# Shell scripts run in a Linux container; CRLF would break the shebang.
*.sh text eol=lf
Dockerfile text eol=lf

# Never let autocrlf near the records the first deploy backfills.
*.parquet binary
*.ico binary
