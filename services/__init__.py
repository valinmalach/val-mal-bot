"""What the bot does, as opposed to how it is reached.

Nothing is re-exported here: import from the module that owns the name, so a
reader lands on the file that defines it rather than on a list of thirty.
"""
